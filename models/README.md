# Models

Model weights are intentionally excluded from Git.

The default configuration uses `yolo11n.pt`. Ultralytics can download supported pretrained weights when first used, depending on the installed version and environment.
